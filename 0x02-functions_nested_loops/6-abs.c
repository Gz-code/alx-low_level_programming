#include "main.h"
/**
* _abs - Computers the absolute value
* of an integer.
*
* @i: The character in ASCII code
*
* Return: absolute value
*/
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}

	else
	{
		return (i * -1);
	}

}

